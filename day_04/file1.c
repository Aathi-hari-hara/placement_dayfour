#include <stdio.h>

int main(){
    // notes
    // brake is to terminate the loop
    // return is to terminate the hole program
    // continue is used to skip the statement
    // 'any number /10' is used to remove last bit of that number in decimal and for binary it's '/2'
    // while loop -> to run the condition for infinite number of time


    // q1 numbers by usinf for loop 
    int x;
    for(x=0;x<=100;x++){=
        printf("%d \n",x);
    }

    //q5
    printf("%d",156/10);    
    return 0;

}